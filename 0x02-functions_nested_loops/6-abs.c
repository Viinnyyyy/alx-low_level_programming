#include "main.h"

/**
 *_abs - check the code
 *@int: changes any number to positive
 *
 *Return: char.
 **/
int _abs(int);
{
	int n;

	if (n > 0)
	{
		_putchar(n);
	}
	else if (n == 0)
	{
		_putchar(n);
	}
	else
	{
		n = n * -1;
		_putchar(n);
	}

	return (n);
}
