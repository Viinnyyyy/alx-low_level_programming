#include "main.h"
#include <string.h>

/**
 *_strlen - returns string length
 *
 *@s: string value
 *
 *Return: length
 */
int _strlen(char *s)
{
	int len;

	len = strlen(const char *s);
	_putchar(len);

	return (len);
}
