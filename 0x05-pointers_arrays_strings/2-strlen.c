#include <string.h>
#include "main.h"


/**
 *_strlen - returns string length
 *
 *@s: string value
 *
 *Return: length
 */
int _strlen(char *s)
{
	char str;
	int len;

	len = strlen(const char *str);

	return (len);
}
