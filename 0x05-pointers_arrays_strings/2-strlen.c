#include <string.h>
#include "main.h"


/**
 *_strlen - returns string length
 *
 *@s: string value
 *
 *Return: length
 */
int _strlen(char *s)
{
	int len;

	len = strlen(char *s);
	_putchar(len);

	return (len);
}
