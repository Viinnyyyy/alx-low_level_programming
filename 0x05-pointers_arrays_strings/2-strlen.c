#include <string.h>
#include "main.h"


/**
 *_strlen - returns string length
 *
 *@s: string value
 *
 *Return: length
 */
int _strlen(char *s)
{
	char *s[50000];
	int len;

	len = strlen(char *s);

	return (len);
}
