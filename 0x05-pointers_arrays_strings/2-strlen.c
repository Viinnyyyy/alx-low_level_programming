#include "main.h"
#include <string.h>

/**
 *_strlen - returns string length
 *
 *@s: string value
 *
 *Return: length
 */
int _strlen(char *s)
{
	char len[] = *s;
	l = strlen(char s);

	_putchar(l);

	return (len);
}
